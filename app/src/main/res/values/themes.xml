<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AgroShop" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/purple_200</item>
        <item name="hintTextColor" tools:targetApi="l">@color/black</item>
    </style>

    <style name="Theme.AgroShop" parent="Base.Theme.AgroShop" />

    <style name="Base.Theme.AgroShopLogin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor" tools:targetApi="l">#FFCD1A</item>
        <item name="hintTextColor" tools:targetApi="l">#FFFFFF</item>
    </style>

    <style name="Theme.AgroShop.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AgroShop.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AgroShop.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>